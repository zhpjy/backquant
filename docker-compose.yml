services:
  mariadb:
    image: mariadb:11.2
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-backquant_root_pass}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-backquant}
      MYSQL_USER: ${MYSQL_USER:-backquant_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-backquant_pass}
      # MYSQL_EXTRA_DATABASE: not needed, all tables in backquant
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./backtest/db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  backend:
    build:
      context: ./backtest
    image: backquant-backend
    environment:
      CONFIG_ENV: production
      SECRET_KEY: ${SECRET_KEY:-change-me}
      JWT_EXPIRES_HOURS: ${JWT_EXPIRES_HOURS:-24}
      LOCAL_AUTH_MOBILE: ${LOCAL_AUTH_MOBILE:-admin}
      LOCAL_AUTH_PASSWORD: ${LOCAL_AUTH_PASSWORD:-pass123456}
      LOCAL_AUTH_PASSWORD_HASH: ${LOCAL_AUTH_PASSWORD_HASH:-}
      LOCAL_AUTH_USER_ID: ${LOCAL_AUTH_USER_ID:-1}
      LOCAL_AUTH_IS_ADMIN: ${LOCAL_AUTH_IS_ADMIN:-true}
      RQALPHA_BUNDLE_PATH: /data/rqalpha/bundle
      RQALPHA_BUNDLE_BOOTSTRAP: ${RQALPHA_BUNDLE_BOOTSTRAP:-1}
      RQALPHA_BUNDLE_CRON: "${RQALPHA_BUNDLE_CRON:-0 3 1 * *}"
      BACKTEST_BASE_DIR: /data/backtest
      BACKTEST_RENAME_DB_PATH: /data/backtest/backtest_meta.sqlite3
      RESEARCH_NOTEBOOK_ROOT_DIR: /data/notebooks
      RESEARCH_NOTEBOOK_PROXY_BASE: /jupyter
      RESEARCH_NOTEBOOK_API_BASE: http://jupyter:8888/jupyter
      RESEARCH_NOTEBOOK_API_TOKEN: ${JUPYTER_TOKEN:-}
      RESEARCH_PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-}
      # Database configuration (default: sqlite, set to mariadb to enable)
      DB_TYPE: ${DB_TYPE:-mariadb}
      DB_HOST: ${DB_HOST:-mariadb}
      DB_PORT: ${DB_PORT:-3306}
      DB_NAME: ${DB_NAME:-backquant}
      DB_USER: ${DB_USER:-backquant_user}
      DB_PASSWORD: ${DB_PASSWORD:-backquant_pass}
      DB_TABLE: ${DB_TABLE:-dbbardata}
    volumes:
      - backtest_data:/data/backtest
      - rqalpha_bundle:/data/rqalpha/bundle
      - notebooks:/data/notebooks
    depends_on:
      mariadb:
        condition: service_healthy
      jupyter:
        condition: service_healthy
    restart: unless-stopped

  jupyter:
    image: backquant-backend
    environment:
      JUPYTER_TOKEN: ${JUPYTER_TOKEN:-}
      RQALPHA_BUNDLE_BOOTSTRAP: "0"
      RQALPHA_BUNDLE_CRON: "off"
    command: >
      jupyter lab --ip=0.0.0.0 --port=8888 --no-browser
      --allow-root
      --ServerApp.base_url=/jupyter
      --ServerApp.root_dir=/data/notebooks
      --ServerApp.token=${JUPYTER_TOKEN:-}
      --ServerApp.password=
      --ServerApp.allow_origin=*
      --ServerApp.allow_credentials=True
    volumes:
      - rqalpha_bundle:/data/rqalpha/bundle
      - notebooks:/data/notebooks
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8888/jupyter/api"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VUE_APP_API_BASE: ${VUE_APP_API_BASE:-}
        VUE_APP_API_SERVER: ${VUE_APP_API_SERVER:-}
    image: backquant-frontend
    ports:
      - "${FRONTEND_PORT:-8088}:80"
    depends_on:
      backend:
        condition: service_started
      jupyter:
        condition: service_healthy
    restart: unless-stopped

volumes:
  mariadb_data:
  backtest_data:
  rqalpha_bundle:
  notebooks:
